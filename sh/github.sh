#!/bin/sh

# Dependencies:
# - Install `gh` and authenticate with your GitHub account
#
# gh repo create [repo-name] --public --clone
# This creates the repository on GitHub, sets it to public, and clones it to your local machine.
#
# gh repo create [repo-name] --private --source=. --remote=origin --push
# This initializes a repository on GitHub, adds it as a remote to your local folder, and pushes your current code.
